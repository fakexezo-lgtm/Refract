Objective:
Fix client data not appearing in Tasks and Pipeline (client selection showing “No client found”).

Context:
Clients are successfully created and visible in Clients page, but not accessible in Tasks/Pipeline selectors.

Requirements:

Ensure clients are correctly fetched from database in:
Tasks module
Pipeline module
Fix client query logic:
Use correct table
Ensure proper data mapping (id, name, etc.)
Ensure client list is:
Loaded before rendering dropdown/select
Passed correctly to components
Fix filtering/search logic:
Remove incorrect filters returning empty state
Ensure consistent data source:
Same client dataset used across app
Handle loading state:
Prevent showing “No client found” before data loads

Constraints:

Do NOT change UI
Do NOT change database schema
Only fix data fetching, state, and binding

Output:

Clients visible in Tasks and Pipeline selectors
No false “No client found” state
Consistent client data across all modules